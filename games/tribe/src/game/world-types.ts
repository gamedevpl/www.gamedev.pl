/**
 * Defines the core data structures and type definitions for the Tribe game world state,
 */

export type GameWorldState = {
  time: number; // Current time in seconds since the start of the game
};
