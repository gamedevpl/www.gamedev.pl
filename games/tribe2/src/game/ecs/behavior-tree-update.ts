import { Entity, GameWorldState } from '../types/world-types';

/**
 * Placeholder for behavior tree update logic.
 * @param _entity The entity to update.
 * @param _state The current game world state.
 * @param _deltaTime The time elapsed since the last frame in seconds.
 */
export function updateBehaviorTree(_entity: Entity, _state: GameWorldState, _deltaTime: number): void {
  // TODO: Implement behavior tree update logic.
}
