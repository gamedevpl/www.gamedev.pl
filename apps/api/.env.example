# Which game generator to use. Only "mock" is implemented in the MVP slice.
LLM_PROVIDER=mock

# Server binding
PORT=3001
HOST=127.0.0.1

# Placeholders for future real LLM providers (not used yet):
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
