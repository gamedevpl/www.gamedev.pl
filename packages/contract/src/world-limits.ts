// Limits on a game's shared world entry.

// Fields one world entry may carry.
export const MAX_WORLD_FIELDS = 12;

// Longest a world field key may be.
export const MAX_WORLD_KEY_LENGTH = 64;

// One world entry's fields, encoded as UTF-8 JSON.
export const MAX_WORLD_ENTRY_BYTES = 4 * 1024;
