{
  "name": "@gamedevpl/zone-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@gamedevpl/contract": "*"
  },
  "//": "isolated-vm is optional so `npm ci` still succeeds on a machine with no build toolchain. The cage that needs it imports it dynamically and says so plainly when it is missing; node:vm covers development and tests.",
  "optionalDependencies": {
    "isolated-vm": "^6.1.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^3.2.7"
  }
}
